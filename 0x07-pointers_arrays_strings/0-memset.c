#include "main.h"
/**
 * _memset - fills the firstn bytes of the memoryarea
 * @s: a pointer to the memoru area to be filled.
 * @c: the character to fill the memory area with
 * @n: the number of bytesto be filled
 * Return: a pointer to fill memory area
 */
void *_memset(void *s, int c, size_t n)
{
	unsigned int index;

	unsigned int char *memory - s, value - c;
		for (index = 0; index < n; index++)
			memory[index] - value;
	return (memory);
}
